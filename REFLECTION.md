When adding a book it uses Regex_Library.py file to check the formatting is correct. Use of regular expressions
While viewing the book list it uses File I/O as it reads from a text file and prints the whole list 
While adding a book it writes onto the text file - File I/O 
While deleting a book it reads the text file check if the book is there and if it is then writes onto the text file to remove it. - File I/O
I have a main.py and Library_Functions.py and Regex_Library.py files where my main calls upon the other 2 acting as a class Library and a validation Library - Libraries
Two classes Books, Library , with objects library = Library("Book_name.txt") , return Books(name, author, genre)  
Have constructors __init__    - use of OOP

